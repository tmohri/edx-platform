;(function (define) {
    'use strict';
    define(['backbone'],
        function(Backbone) {

        var ShareModalModel = Backbone.Model.extend({});
        return ShareModalModel;
    });
}).call(this, define || RequireJS.define);
